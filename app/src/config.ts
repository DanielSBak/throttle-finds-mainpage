// Repository the app publishes to. The website is rebuilt automatically
// by GitHub Pages after every commit the app makes.
export const OWNER = 'DanielSBak';
export const REPO = 'throttle-finds-mainpage';
export const BRANCH = 'main';

// Optional: "Sign in with GitHub" (device flow). Create an OAuth App at
// https://github.com/settings/applications/new (any callback URL works,
// enable Device Flow) and paste its Client ID here. If left empty, the
// login screen only offers signing in with a personal access token.
export const GITHUB_OAUTH_CLIENT_ID = '';

export const SITE_URL = 'https://throttlefinds.com';
